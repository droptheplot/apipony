- @documentation.sections.each do |section|
  .section
    %h1
      = section.title
    - section.endpoints.each do |endpoint|
      .endpoint
        %h2
          = endpoint.url
        .description
          = endpoint.description
